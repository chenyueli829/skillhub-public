---
alwaysApply: true
---

## 提问存档（全局强制）

本工作区内**所有对话**必须经过 **`@cursor-query-log`**：先阅读并遵守 `.cursor/skills/cursor-query-log/SKILL.md`，在**每轮回复用户之后**，将**本轮用户消息的原文**按该 skill 规定的格式追加到同目录下的 `queries-log.md`（只追加、勿整文件覆盖）。禁止因消息过短或寒暄而跳过；唯一例外以该 `SKILL.md`「何时记录」一节为准。

## 验证Skill（全局强制）
如果用户艾特了某个skill时，代表用户希望验证这个skill，你必须遵守：

- 互不干扰：这个仓库主要是 skill验证，不要相互干扰。当你在验证某一个skill时，仅关注该skill的文件夹。不用读取其他文件夹的内容。保证上下文干净。
- 必须使用skill，不要擅自做主，如果skill异常，则直接停止并报错，严禁自己回答。
- 输出结果：保存到该skill对应文件夹下，新建一个`outputs`文件夹，并保存输出内容
